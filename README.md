# tutorial
provide tip for all tools

1. Using virtualenv
  mac:
    $mkdir [project name]
    $cd [project name]
    $virtualenv venv
    $source venv/bin/activate
    (venv)$....
    
    to deactivate
    (venv)$deactivate
